# phpIPAM Extension for CFME

## Prerequesties

In phpIPAM take the following steps:

1. Create a user with admin permissions and a password 

2. Enable API

3. Create an API key 

Install the httparty ruby gem on the CFME server

```
gem install httparty
```
